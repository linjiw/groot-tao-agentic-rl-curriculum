# Progress Log — 2026-06-30

> Daily catch-up note. Read this + [`docs/progress/NEXT.md`](NEXT.md) first thing tomorrow.

## TL;DR

Built the repo, wrote the full design plan, and got the flagship experiment **coded, statically
validated, and launch-ready** — but it **has not been trained** (this box is a single A10G with
no Isaac Lab; the flagship needs a 64+-GPU Isaac-Lab host). The single most important result:
**Step-0 verification passed** — the Stage-2 curriculum mechanism is confirmed viable in source.

## Where everything lives

- **Repo:** https://github.com/linjiw/groot-tao-agentic-rl-curriculum (public, fork of `NVIDIA-TAO/tao-skills-bank`).
- **Working clone on this box:** `/home/ec2-user/work/groot-tao-agentic-rl-curriculum`
- **GR00T/Isaac source (read for analysis, pinned as submodules):**
  - `/workspace/GR00T-WholeBodyControl` → submodule `external/GR00T-WholeBodyControl` @ `0e35637`
  - `/workspace/Isaac-GR00T` → submodule `external/Isaac-GR00T` @ `ab88b50`
  - `/workspace/IsaacLab` (v2.3.2) — present locally, **not** added as a submodule.
- **Design plan:** `docs/design/00-overview.md` … `06-risks-and-open-questions.md`
- **Flagship experiment artifacts:** `experiments/stage2-termination-curriculum/`
- **Memory notes (for the assistant):** `~/.claude/projects/.../memory/` —
  `cosmos-rl-agentic-posttraining.md`, `groot-wbc-tao-bridge.md`, `tao-skills-bank-overview.md`.

## What got done today

### 1. Investigation (verified by reading source, not memory)
- **TAO "agentic post-training" skill = `tao-finetune-cosmos-reason`** (Cosmos-RL container). Exposes
  only SFT (`train_policy.type` enum locked to `['sft']`); RL is latent. Agentic AutoML layer has
  `autoresearch`/`pbt`/`llm`; DEFT is the failure-driven data loop.
- **GR00T N1.7 VLM backbone = `nvidia/Cosmos-Reason2-2B` (Qwen3-VL)** — same family TAO LoRA-SFTs
  (`qwen3_backbone.py`). **SONIC's RL core subclasses HuggingFace TRL's `PPOTrainer`**
  (`ppo_trainer.py:321`, `trl==0.28.0`). These two facts are the whole bridge.

### 2. Repo created + combined
- Forked `NVIDIA-TAO/tao-skills-bank` → `linjiw/groot-tao-agentic-rl-curriculum` (renamed, public).
- Added GR00T + Isaac-GR00T as **submodules** pinned to the analyzed commits (no weights/LFS vendored;
  NVIDIA attribution intact). README rewritten; original preserved as `README.tao-upstream.md`.
- Wrote the 7-doc design plan (`docs/design/`): how GR00T works; 6-stage curriculum plan; two-track
  agentic-RL plan; TAO cross-pollination (both directions); flagship experiment; risks/open-questions.
- Every section had passed an adversarial critique that corrected hallucinated line-numbers + one
  fabricated metric (`open_loop_eval.py` outputs MSE/MAE, **not** `success_rate`).

### 3. First infra step — Stage-2 termination-threshold curriculum
- **Env recon:** 1× A10G (23 GB), 8 vCPU, 30 GB RAM. `.venv_sim` = MuJoCo venv (torch+CUDA, imports
  `gear_sonic`, but **no `isaaclab`**). Training path requires Isaac Lab → not runnable here.
- **Step-0 VERIFIED ✅:** `TerminationManager.compute()` reads `term_cfg.params` **fresh every step**
  (`external/IsaacLab/.../termination_manager.py:167`); `set/get_term_cfg` exist. So a `modify_term_cfg`
  curriculum write propagates at episode boundaries. **Stage 2 is viable.**
- **Two design-doc corrections** (found during verification, now reconciled in `05-flagship-experiment.md`):
  `modify_term_cfg` is an **IsaacLab** primitive re-exported into the `gear_sonic` mdp namespace (not a
  `gear_sonic` function); its `s.`-shorthand rewrites `terminations.` → `termination_manager.cfg.`;
  `modify_fn` signature is `(env, env_ids, old_value, **params)` returning `NO_CHANGE` to skip redundant
  writes.
- **Shipped, launch-ready artifacts** in `experiments/stage2-termination-curriculum/`:
  - `patch/stage2_termination_curriculum.patch` — adds 3 `CurriculumCfg` fields, the injection block, and
    a `step_curriculum_nochange` helper. `git apply --check`: **clean**.
  - `config/threshold_tighten.yaml` — 3 `modify_term_cfg` terms (anchor_pos, ee_body_pos, foot_pos_xyz),
    tightening 0.30→0.22→0.15 (foot 0.35→0.28→0.20) on a milestone schedule.
  - `apply_stage2.py` (idempotent, anchor-based) + `apply_and_validate.sh` (CPU-only static checks).
  - `00-env-and-step0-verification.md`, `RUN.md`.
- **All CPU-only static checks pass:** AST-compile of both edited files, YAML parse (3 well-formed terms),
  `step_curriculum_nochange` milestone unit test (6/6).
- Submodule stays **pinned and clean** — changes live as a reviewable patch+config in our repo, applied to
  the WBC checkout only at run time.

## Git state at end of day
- Branch `main` @ `918719d` (pushed). Commits today: `b049df6` (design plan), `918719d` (Stage-2).
- Submodules pinned: WBC `0e35637`, Isaac-GR00T `ab88b50`. Working tree clean.
- `upstream` remote → `https://github.com/NVIDIA-TAO/tao-skills-bank.git`.

## The blocker (one sentence)
**We need an Isaac-Lab multi-GPU host to actually train;** the A10G dev box can only do design + code +
CPU-only validation, which is now complete for Stage 2.

## Decisions still open (carried to tomorrow)
- Repo is **public** and still shows as a **fork** of NVIDIA-TAO (rename doesn't detach). Decide whether
  to keep public/fork or go private/standalone.
- No training host secured yet. Tomorrow's plan branches on whether one is available (see `NEXT.md`).
