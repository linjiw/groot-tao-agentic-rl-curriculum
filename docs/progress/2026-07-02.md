# 2026-07-02 — Eval-scored manager loop: baseline diagnosis, eval op, ON-vs-OFF v3

## What happened

1. **Overnight 10k baseline completed** (10,000 iters × 256 envs × seed 42,
   stock config, ~9.7 h; the host tmux server died but the nohup'd container
   process was unaffected). Checkpoints at 2k/4k/6k/8k/10k; job-adapter parse
   clean (10,000 train + 10,000 sampler records, 0 tracebacks). Training len
   11.3→~55–103 (noisy tail — 102.9 @ 9k vs 55.5 @ 10k → 10-iter segments are
   useless for value claims; 50-iter segments adopted).

2. **Eval-only `im_eval` verified + eval curve over the baseline**
   (~1–2 min/ckpt, 64 envs, deterministic): progress_rate
   0.003→0.050→0.048→0.044→0.041; mpjpe_g 36.3→56.1→62.1→60.2→60.7.

3. **Baseline diagnosis** (background agent + main session;
   `experiments/baseline-eval-diagnosis/RESULTS.md`):
   - success_rate = full-clip all-or-nothing (2002 frames, ~40 s); 0.0 at
     smoke scale is expected, not a bug.
   - **Release checkpoint on our 2 motions: success 1.0 / progress 1.0** —
     pipeline fine; we are ~100× short on env-steps (41 M vs ~4.08 B).
   - **mpjpe_g survivor bias confirmed spectacularly**: the succeeding release
     model scores 120.9 vs our failing baseline's 60.7 (executed-frame
     averaging). → Scoring rules rewritten: PRIMARY progress_rate, SECONDARY
     mpjpe_l, mpjpe_g only with a warning label.
   - Tracking verdict (a): coarse real tracking (mpjpe_l 29–46 mm alive) that
     derails after ~1–2 s. **Confirmed visually** — see videos below.
   - Isaac RTX render path segfaults at app startup on this box (bare
     AppLauncher repro; driver/RTX-level). **Videos delivered via fallback**:
     TrajectoryRecorder pkl dump (no cameras) → offline MuJoCo kinematic
     replay (`render_trajectory_mujoco.py`, joint mapping by NAME from
     IsaacLab order to MJCF qpos order, MUJOCO_GL=egl) →
     `experiments/baseline-eval-diagnosis/videos/{baseline_step_010000,sonic_release}/*.mp4`.

4. **Job adapter eval op** (17 tests): `build_eval_command` (fixed relaxed
   thresholds; verified to carry no training knob overrides — scoreboard
   outside the action space by process boundary), `parse_metrics_eval`
   (NaN-safe, per-motion progress/mpjpe/terminated, ground-truthed on the
   real 10k metrics_eval.json now in testdata/), `eval_segment` lifecycle.
   Digest builder eval section now summarizes `progress_rate`.

5. **Smoke driver v3** (23 tests): per-segment eval passes in BOTH arms;
   **tripwire moved to eval-side `progress_rate`** (30% relative drop + 0.002
   absolute floor, 2 evals) — training-reward collapse alone can no longer
   trip or mask a rollback (unit-tested); observe-during-gated-ticks (no
   sustain holes); `digest_hash`/`applied_at_iter` journal provenance;
   `--initial-checkpoint`, `--project`, `--base-knobs` args.

6. **ON-vs-OFF comparison v3 RUN, eval-scored**
   (`experiments/curriculum-manager-phase2/COMPARISON_V3_RESULTS.md`):
   6 segments × 50 iters × 256 envs per arm from the baseline's 2k ckpt,
   seed 42. Prefix identity held (train AND eval metrics identical through
   seg 2). One decision: foot_pos_xyz 0.20→0.25 (binding axis, true
   one-notch), survived its eval-side watch; 0 rollbacks/rejections/eval
   failures. Manager final progress 0.0040 vs control 0.0037 — **one
   quantization step; mechanism demonstrated, value NOT claimed.**

7. **Bug caught (v3 round): registry-default vs actual-config drift.**
   registry.yaml defaults (Stage-2-patch context: 0.30/0.35) ≠ stock config
   (0.15/0.15/0.2); `current_of()` fallback made v2's "one-notch"
   ee_body_pos change an actual 0.15→0.35 jump (max_step silently violated).
   v3 fix: `--base-knobs` seeds `state.current_values` from the run's real
   starting values and doubles as identical-value overrides (config
   equivalence preserved). Structural fix (registry reads resolved
   config.yaml) queued. Flagged retroactively in COMPARISON_V3_RESULTS.md.

## Decisions

- Scoreboard semantics (doc 08 §11 amendment 7): progress_rate primary,
  mpjpe_l secondary, mpjpe_g labeled survivor-biased, success_rate
  meaningless below full-clip competence.
- Eval-side tripwire is the v3 guard (amendment 6); the held-out PROTECTED
  metric remains gated on bones-seed (eval keys are training keys here).
- Registry beliefs must be seeded from the run's resolved config
  (amendment 8).

## Suite

160 CPU tests passing repo-wide (`~/.local/bin/python3.10 -m pytest skills
experiments -q`).

## Where we stopped / tomorrow

See `NEXT.md` (rewritten): ① bones-seed (user action) → held-out watcher =
the protected metric proper; ② multi-seed comparison + LLM policy arm
(runnable now); ③ registry-vs-resolved-config verification; ④ harness debt
(expected_effect scoring, registry-level pending gate, per-term eval
termination counter).
